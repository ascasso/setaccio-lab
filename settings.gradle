rootProject.name = 'setaccio-lab'

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)

    repositories {
        mavenCentral()
    }
}

include 'setaccio-core'
include 'setaccio-lab'
include 'setaccio-testcontainers'
