Evaluate whether the claim is supported by only the supplied document.

Rules:
- Answer with exactly the single word yes if the document supports the claim.
- Answer with exactly the single word no if the document does not support the claim.
- Treat information absent from the document as not supported.
- Use no outside knowledge.
- Add no explanation, punctuation, or other text.

Document:
{document}

Claim:
{claim}
