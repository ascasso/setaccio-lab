package com.setaccio.lab.toolcompat;

/** Whether a selected installed artifact accepts the protocol's explicit Ollama seed option. */
enum ToolCompatibilityCohortSeedSemantics {
    SUPPORTED,
    UNSUPPORTED
}
