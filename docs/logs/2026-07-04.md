# 2026-07-04

## Tool search planning and config

### Summary
- Added the Spring AI Tool Search Advisor starter to `setaccio-lab` for future tool-calling benchmark comparisons.
- Kept Tool Search Advisor disabled by default in normal app config and test config.
- Documented the `SETACCIO_LAB_TOOL_SEARCH_ENABLED` and `SETACCIO_LAB_TOOL_SEARCH_INDEX_TYPE` mappings in `docs/ENVIRONMENT.md`.
- Added test-plan notes for comparing standard tool calling with Tool Search Advisor behavior while keeping default tests offline.

### Verification
- Covered by the Gradle build hygiene verification below.

## Deterministic tool fixtures

### Summary
- Added public-safe Spring AI tool fixtures for arithmetic, fixed time, and small catalog lookup scenarios.
- Published the fixtures through a `MethodToolCallbackProvider` so future standard tool-calling and Tool Search Advisor comparisons can share the same tool definitions.
- Kept the fixture time source deterministic through `SETACCIO_LAB_TOOL_FIXTURE_INSTANT`, defaulting to `2026-01-15T12:00:00Z`.
- Updated test-plan and environment documentation for the new tool fixtures.

### Verification
- `./gradlew :setaccio-lab:test`
- `./gradlew :setaccio-lab:build`

## Standard tool-calling benchmark

### Summary
- Added a local-only `POST /api/lab/tools` JSON endpoint for explicit Ollama tool-calling benchmark runs.
- Added tool benchmark request/result/row models that capture provider/model metadata, prompt id/text, advisor mode, requested tools, selected tool calls, executed tool responses where Spring AI exposes them, tool errors, latency, success/failure, token counts when available, and output text.
- Implemented the standard Spring AI `ToolCallingAdvisor` path with the deterministic arithmetic, fixed-time, and catalog fixture tools.
- Added default public-safe benchmark prompts for arithmetic, fixed UTC time, time-zone conversion, catalog lookup, and catalog listing.
- Persisted tool benchmark results as `*-tool-calling.json` under the configured lab results directory, defaulting to `build/lab-results/`.
- Left Tool Search Advisor comparison as the next slice because this implementation intentionally narrows on the standard advisor path first.
- Kept automated coverage mocked/offline; no live Ollama integration tests or default/CI live provider paths were added.

### Verification
- `./gradlew :setaccio-lab:test`

## Tool-calling benchmark architecture follow-up

### Summary
- Changed tool-calling token reporting to accumulate Spring AI usage metadata from every `ChatResponse` observed by the standard advisor loop, instead of reading only the final answer response.
- Kept token fields nullable when Spring AI does not expose usage metadata for a response.
- Moved tool callback injection from manually built `OllamaChatOptions` to the portable `ChatClient` request API while preserving per-request model selection.
- Kept the benchmark provider path local Ollama for this slice; no new provider implementation or live integration test path was added.

### Verification
- `./gradlew :setaccio-lab:test`

## Tool-calling benchmark review polish

### Summary
- Updated the tool benchmark failure log message to label prompt identifiers as `promptId`.
- Added service-test assertions that the default requested tool set matches the available deterministic tool callbacks.
- Left controller validation extraction and configurable default prompts as future cleanup rather than expanding this small review pass.

### Verification
- `./gradlew :setaccio-lab:test`

## Gradle build hygiene

### Summary
- Removed the explicit `jackson-datatype-jsr310` catalog version so Spring Boot/Spring AI dependency management controls the Jackson datatype version with the rest of the Jackson stack.
- Moved the direct `bcprov-jdk18on` version into `gradle/libs.versions.toml`.
- Added a `setaccio-core` verification task that fails when runtime dependencies include Spring Framework, Spring Boot, or Spring AI artifacts.
- Changed `setaccio-testcontainers` to depend on `setaccio-lab` only for tests because the module currently has no main runtime fixture/API sources.
- Enabled `RepositoriesMode.FAIL_ON_PROJECT_REPOS` in `settings.gradle` after confirming repository declarations are centralized.

### Verification
- `./gradlew :setaccio-core:check :setaccio-lab:dependencies :setaccio-testcontainers:dependencies`
- `./gradlew :setaccio-core:build :setaccio-lab:build :setaccio-testcontainers:build`
- `./gradlew :setaccio-lab:dependencyInsight --dependency jackson-datatype-jsr310 --configuration runtimeClasspath`
- `./gradlew :setaccio-core:dependencies --configuration runtimeClasspath`
- `./gradlew allDeps`
- `git diff --check`

## README current capabilities polish

### Summary
- Replaced the older single-track current-focus wording with a tighter `Current capabilities` section.
- Documented both local benchmark endpoints consistently: `POST /api/lab/vision` and `POST /api/lab/tools`.
- Kept the public-safe, local-first, offline-safe framing explicit for default builds and generated benchmark outputs.

### Verification
- `git diff --check -- README.md docs/logs/2026-07-04.md`
