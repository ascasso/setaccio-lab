# 2026-07-27

## TODO: Gradle task project access deprecation

- `:setaccio-lab:visionMatrixVerify` reports Gradle's deprecation warning for
  execution-time `Task.project` access. The immediate source is
  `buildSrc/src/main/java/com/setaccio/gradle/VisionMatrixOfflineTask.java`,
  where the task action calls `getProject().getProjectDir()`.
- The same execution-time project-directory pattern is present in the other
  custom matrix and smoke task wrappers and should be addressed consistently.
- Future fix: capture the project directory through a configuration-safe
  injected layout or directory property, without changing the task's working
  directory or command-line behavior. Do not suppress the warning as the fix.
- This remains documentation only; no implementation change was made here.
