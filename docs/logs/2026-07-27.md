# 2026-07-27

## TODO: Gradle task project access deprecation

- `:setaccio-lab:visionMatrixVerify` reports Gradle's deprecation warning for
  execution-time `Task.project` access. The immediate source is
  `buildSrc/src/main/java/com/setaccio/gradle/VisionMatrixOfflineTask.java`,
  where the task action calls `getProject().getProjectDir()`.
- The same execution-time project-directory pattern is present in the other
  custom matrix and smoke task wrappers and should be addressed consistently.
- Future fix: capture the project directory through a configuration-safe
  injected layout or directory property, without changing the task's working
  directory or command-line behavior. Do not suppress the warning as the fix.
- This remains documentation only; no implementation change was made here.

## Private vision human-review preparation

- Added the offline-only `visionHumanReviewPrepare` task to replace the manual
  verification, comparison, corpus lookup, and response-grouping sequence with
  one explicit command.
- The task requires explicit baseline, candidate, and fixed ignored corpus
  paths. It verifies both evidence directories, enforces deterministic
  comparability, and requires the current corpus MIME/BLAKE3 identities to
  match the saved inputs before writing anything.
- It writes one ignored, non-overwriting `HUMAN-REVIEW.md` worksheet under
  `build/vision-human-review/`, including private references, local image links,
  paired raw responses, and blank fields from the committed rubric. Exact
  successful repetitions are shown once; differing or failed repetitions stay
  separate.
- The task does not start Spring, contact Ollama, select runs, score semantics,
  or make an adopt/revise/reject decision.
- Its custom Gradle wrapper receives configured directory properties and does
  not call `Task.project` during execution. The earlier TODO remains open for
  the pre-existing matrix and smoke wrappers that still use that deprecated
  pattern.

### Verification

- Confirmed the repository wrapper is Gradle `9.6.1`.
- `./gradlew :setaccio-lab:visionMatrixTest --tests
  com.setaccio.lab.vision.VisionHumanReviewPreparerTest --stacktrace`
- `./gradlew :setaccio-lab:visionMatrixTest --rerun-tasks`
- `./gradlew :setaccio-lab:test :setaccio-lab:visionMatrixTest --rerun-tasks`
- `./gradlew :setaccio-core:build :setaccio-lab:build
  :setaccio-testcontainers:build --rerun-tasks`
- Ran `./gradlew help --task
  :setaccio-lab:visionHumanReviewPrepare --configuration-cache --warning-mode
  all` twice; the first invocation stored the configuration cache and the
  second reused it without a deprecation warning.
- `git diff --check`
- No live Ollama or remote-provider call ran. The ignored private corpus was
  not copied into tracked files, and no human semantic judgment was performed.

## Human-review command guidance correction

- Corrected `docs/VISION-HUMAN-REVIEW.md` so it no longer names the preparation
  task without its required arguments. The guide now spells out the exact
  baseline directory, candidate directory, preparation command, and generated
  worksheet path for the current Prompt v1/v2 review, and stops clearly when
  either ignored evidence directory is absent.
- Expanded that guide into a complete start-to-finish human workflow: inspect
  the private reference first, judge baseline and candidate independently,
  handle exact or differing repetitions, complete the pair comparison, choose
  adopt/revise/reject, and publish only safe human-reviewed aggregates.
- Replaced historical run names in the environment example with explicit
  current controlled-run names and corrected the earlier shortened candidate
  path so the command can be copied without choosing or translating paths.
- Improved a bare `visionHumanReviewPrepare` invocation to report all three
  required options and point directly to the review guide. Explicit run
  selection remains mandatory; the task does not guess the newest evidence.

### Verification

- Ran bare `./gradlew visionHumanReviewPrepare` and confirmed its expected
  failure now reports all three required options and the review-guide pointer.
- `./gradlew :setaccio-lab:visionMatrixTest help --task
  :setaccio-lab:visionHumanReviewPrepare --configuration-cache --warning-mode
  all`
- No live model or provider call ran.

## Canonical human-review guide consolidation

- Kept `docs/VISION-HUMAN-REVIEW.md` as the single human-review policy and
  operator guide instead of adding a second tracked workflow document.
- Added a compact preflight summary for privacy, workload, task boundaries, and
  non-overwriting behavior.
- Reorganized each model/case review into one fixed reference, baseline,
  candidate, comparison, and repeat sequence.
- Added explicit judgment-entry formats, a public-safe unsupported-specificity
  example, and consistent pair-comparison vocabulary.
- Clarified that the ignored private worksheet may retain the case-level
  rationale needed to audit the decision, while public closeout remains limited
  to scrubbed aggregate counts and qualitative patterns.

### Verification

- `git diff --check`
- Documentation-only change; no live model or provider call ran.

## Vision comparison framework provenance gate

- Corrected `visionMatrixCompare` to reject baseline and candidate manifests
  whose Spring Boot or Spring AI versions differ. Matching execution-engine and
  protocol settings no longer allow framework provenance drift through the
  comparison gate.
- Because `visionHumanReviewPrepare` uses the same comparator, it now also
  refuses to create a worksheet for runs produced with different framework
  versions. This preserves the documented invariant that only prompt identity
  and code baseline may differ.
- Added regression coverage that independently changes the saved Spring Boot
  and Spring AI versions while leaving the verified run artifacts and locked
  protocol unchanged.

### Verification

- `./gradlew :setaccio-lab:visionMatrixTest --tests
  com.setaccio.lab.vision.VisionMatrixComparisonTest`
- `./gradlew :setaccio-lab:visionMatrixTest :setaccio-lab:build
  --rerun-tasks`
- `git diff --check`
- No live Ollama or remote-provider call ran.

## Local evaluation and Testcontainers planning gate

- Reviewed the checked-out Spring AI `2.0.0` evaluator sources and current
  upstream evaluation guidance. `FactCheckingEvaluator` is the narrow fit for
  the existing claim/context fixture shape; `RelevancyEvaluator` remains
  deferred until the lab has a real retrieval flow and preserved retrieved
  context. The built-in fact-check evaluator reduces its raw answer to a
  boolean with empty feedback/metadata, so the future evidence path must record
  raw response and usage data through a narrow judge-model boundary before
  evaluator normalization.
- Defined the first future live slice as a host-Ollama fact-checking matrix with
  one explicit already-installed judge, full resolved model digest, a tracked
  prompt ID/version/SHA-256, six balanced public fixtures, two seeded
  repetitions, exact verdict parsing, named hard failures, model-behavior
  diagnostics, and shared-manifest offline verification/reanalysis.
- Kept judge verdict, fixture-expectation agreement, repetition consistency,
  label tendency, latency, tokens, and infrastructure failures separate. The
  plan does not treat a local judge as human review, a general factuality
  score, or a model-ranking mechanism.
- Verified the current `setaccio-testcontainers` classpath resolves Spring AI's
  Testcontainers support and Spring Boot service connections but does not
  include the typed Testcontainers Ollama module. Any future `OllamaContainer`
  dependency and explicit container task remain isolated in that module.
- Recorded the full recommendation and future acceptance criteria in
  `docs/LOCAL-AI-EVALUATION-PLAN.md`, and aligned the README, environment guide,
  test plan, module guide, changelog, repository agent guide, and ignored
  plan set.

### Boundaries

- Planning and compatibility review only. No evaluator implementation, judge
  call, Docker access, container runtime task, model pull, or dependency change
  was made.
- The existing deterministic `POST /api/lab/evaluations` endpoint and default
  offline build lifecycle are unchanged.

### Verification

- `./gradlew :setaccio-testcontainers:dependencies --configuration
  testRuntimeClasspath`
- `./gradlew :setaccio-testcontainers:dependencyInsight --dependency
  spring-boot-testcontainers --configuration testRuntimeClasspath`
- `./gradlew :setaccio-testcontainers:dependencyInsight --dependency
  testcontainers-ollama --configuration testRuntimeClasspath` (confirmed no
  resolved dependency)
- `./gradlew :setaccio-core:build :setaccio-lab:build
  :setaccio-testcontainers:build --rerun-tasks`
- `git diff --check`
- No live Ollama or remote-provider call ran.

## Plan closeout and human-review handoff

- Closed the ignored `plan/augustplan.md` work after completing Slices 1
  through 4, 6, and 7.
- Moved the still-incomplete actual human Prompt v1/v2 decision into the new
  active ignored `plan/july2026.md`. The handoff preserves the committed rubric,
  immutable evidence prerequisites, adopt/revise/reject gate, private/public
  boundary, and explicit Git closeout boundary.
- The plan transfer does not relabel the preserved agent-assisted pass as human
  review, does not complete the decision, and does not change Prompt v1 as the
  operational interactive default.
- Added queued `plan/august2026.md` as the real August cycle. It turns the Slice
  7 recommendation into one bounded host-Ollama fact-checking experiment with
  contract, invocation, evidence, runner, controlled-run, interpretation, and
  Testcontainers-justification slices. Retrieval, container implementation,
  remote providers, multiple judges, and alternate Tool Search work remain
  explicitly deferred.

## Tracked human-review operator companion

- Added `docs/VISION-HUMAN-REVIEW-OPERATOR.md` as a committed top-to-bottom
  execution companion after the user explicitly selected it for tracking.
- Kept `docs/VISION-HUMAN-REVIEW.md` authoritative for policy and rubric
  substance, linked the two documents, and required future policy changes to
  align the companion in the same change.
- Replaced an example that repeated the prior agent-assisted `11/12` result
  with neutral `X` / `Y` placeholders so the operator sheet cannot seed the
  still-pending human judgments.
- Confirmed the sheet contains only public-safe workflow paths and examples;
  private images, reference observations, raw responses, and completed
  worksheet judgments remain ignored.
