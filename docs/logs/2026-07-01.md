# 2026-07-01

## Gradle deprecation cleanup

### Summary
- Replaced implicit parent-project property lookups in `build.gradle` with explicit `rootProject.ext` references.
- This removes the Gradle problems report emitted during `./gradlew clean`.
- Superseded later the same day by the Gradle version catalog migration, which removed the root `ext` version block.

### Verification
- `./gradlew clean --warning-mode all`

## Gradle version catalog documentation

### Summary
- Added `gradle/libs.versions.toml` for shared dependency aliases and versions.
- Updated module build scripts to use version catalog aliases for shared libraries, Spring starters, Spring AI BOMs, and test dependencies.
- Moved `mavenCentral()` repository declaration from the root build into `settings.gradle` dependency resolution management.
- Removed the root `ext` version block, root-wide test dependency injection, and root-wide `commons-codec`/`slf4j-api` resolution-strategy block.
- Documented the version catalog setup in `README.md` and `CHANGELOG.md`.
- Clarified that shared dependency version updates now belong in `gradle/libs.versions.toml`.
- Noted that root plugin versions and a few implementation-specific dependencies are still declared in build scripts.

### Verification
- `./gradlew :setaccio-core:test :setaccio-lab:test :setaccio-testcontainers:test`
- `./gradlew :setaccio-core:build :setaccio-lab:build :setaccio-testcontainers:build`

## Documentation drift cleanup

### Summary
- Aligned README and changelog wording with the current Gradle version catalog scope.
- Updated Anthropic environment docs to match the properties already wired in `application.yml`.

### Verification
- Documentation-only change; no additional build verification run.
