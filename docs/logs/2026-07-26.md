# 2026-07-26

## Prompt-v2 contract

- Started the active end-of-July prompt experiment with the offline-only
  versioned prompt contract.
- Preserved the tracked version 1 prompt byte-for-byte and kept it as the
  interactive endpoint default.
- Added tracked prompt version 2 with the bounded instruction to report
  `unknown` rather than infer unsupported exact location, identity, event, or
  time.
- Added a minimal explicit prompt catalog for versions 1 and 2. Unknown or
  missing versions fail clearly; no general prompt-management subsystem was
  introduced.
- Locked the version 2 SHA-256 digest and verified that both versions retain
  the same required output headings.

### Boundaries

- No vision-matrix selection or live model execution was added in this slice.
- No Ollama or remote-provider call was made. The immutable July v1 evidence
  was not moved, changed, or rerun.

### Verification

- `./gradlew :setaccio-lab:test --tests
  com.setaccio.lab.service.VisionPromptContractTest`
- `./gradlew :setaccio-lab:test --tests
  com.setaccio.lab.service.VisionModelInvokerTest`
- `./gradlew :setaccio-lab:test`
- `git diff --check`
