# 2026-07-13

## Tool Search and chat result correctness fixes

- Updated Tool Search trace parsing to extract tool names from Spring AI `ToolSearchResponse.toolReferences` while preserving the existing scalar response formats.
- Updated the chat benchmark to record null or result-less Ollama responses as failed rows with the same no-result error used by the tool benchmark.
- Added focused offline regression coverage for both fixes.
- Verified the complete `setaccio-lab` test suite and `git diff --check`.

### Bounded live verification

- Started the local profile with Tool Search explicitly enabled and confirmed the repository remained configured with `pull-model-strategy: never`; both selected models were already installed locally.
- Sent one-repetition catalog-lookup comparisons with `gemma4:e2b` and `granite4.1:3b`. Both requests returned HTTP 200 and wrote results only under the ignored `build/lab-results/` directory.
- `gemma4:e2b` completed Tool Search but returned an empty discovery array, so it did not exercise non-empty discovery parsing.
- `granite4.1:3b` returned a non-empty scalar discovery array containing `lab_list_catalog_items` and `lab_lookup_catalog_item`. The normalized observation retained both names, `required_tool_discovered` passed, the required lookup tool executed, and the complete case contract passed.
- Spring AI 2.0's live `ToolSearchTool` wrapper exposed scalar arrays in these runs, while the underlying index API uses `ToolSearchResponse`; regression coverage retains both response shapes. No model was pulled and no remote provider was called.

## Opt-in local Tool Search smoke automation

- Added a dedicated `ToolSearchSmokeTask` build task and isolated `toolSearchSmoke` source set so live verification remains outside production code, default tests, `check`, `build`, and default CI.
- Added the explicit `./gradlew toolSearchSmoke --model=<already-installed-model> [--case-ids=...]` workflow. The model is required, semantic case IDs and 1-based ordinals are supported, and missing, blank, duplicate, unknown, or out-of-range selections fail before Spring contacts Ollama.
- The runner uses `ToolBenchmarkCases.defaults()` and the complete `ToolBenchmarkCases.toolNames()` fixture set directly, starts a non-web local Spring context, performs one deterministic paired comparison, forces the regex Tool Search index, and verifies that the effective Ollama pull strategy is `never`.
- Added independent trace-integrity analysis for Spring AI's scalar-array, textual-singleton, and object `toolReferences` response forms. Startup/invocation errors, malformed results, missing trace linkages, and raw-versus-normalized discovery mismatches are hard failures; model behavior categories remain diagnostic.
- Added the required console buckets and warning for no search call, zero matches, non-empty discovery, discovery mismatch, required discovery without execution, and required execution with an output-contract failure.
- Added 11 isolated offline smoke tests for selection, supported wrapper forms, trace integrity, summary categories, invocation failure, malformed data, and non-blocking model behavior. Extended the default trace-evaluator regression coverage for Spring AI's `arg0` query field and a completed zero-match response.
- Documented the task in `README.md`, `docs/ENVIRONMENT.md`, `docs/TEST-PLAN.md`, and `CHANGELOG.md`.
- Verified `./gradlew help --task toolSearchSmoke`, missing-model rejection, invalid ordinal rejection, `./gradlew :setaccio-lab:test :setaccio-lab:toolSearchSmokeTest`, the full three-module build, and `git diff --check`.
- Did not run the live smoke task during this slice because no already-installed model was explicitly selected. No model was pulled and no remote provider was called.

## Post-fix Tool Search diagnostic matrix baseline

### Automation and protocol

- Added the dedicated `toolSearchMatrixBaseline` Gradle task, locked to the July 12 models (`gemma4:e2b`, `granite4.1:3b`, `qwen3.5:0.8b`), canonical cases (`arithmetic-add`, `catalog-lookup`, `catalog-multi-step`, `no-applicable-domain-tool`, `deterministic-tool-failure`), two repetitions, temperature `0.0`, base seed `42` (effective seeds `42`/`43`), no token ceiling, and alternate paired sequential advisor order.
- The runner sources prompts and expectations directly from `ToolBenchmarkCases.defaults()` and exposes the complete `ToolBenchmarkCases.toolNames()` fixture set. It verifies that the deterministic failure expectation is the canonical `fixture-tool-failure` marker before starting Spring.
- Extracted shared Tool Search trace verification for the smoke and matrix paths. Every linked raw array, textual singleton, or object `toolReferences` response must exactly match the normalized discovered tool names; malformed, missing, duplicate, orphaned, or mismatched traces invalidate the run.
- Added exhaustive primary failure classification in this precedence order: no search call, zero discovery, incomplete discovery, discovered-not-executed, execution failure, and output-contract failure. A failed row outside these categories invalidates analysis.
- Added a checked-in July 12 baseline fixture containing both the originally recorded and corrected pass counts, plus explicit request-construction confounder text.
- Added offline coverage for the locked protocol, canonical failure marker, all six failure categories, precedence behavior, successful zero-discovery abstention handling, and raw-versus-normalized mismatch detection.

### Live baseline run

- Confirmed all three exact model IDs were already present in `ollama list`; no model was pulled or downloaded.
- Ran `./gradlew toolSearchMatrixBaseline --output-dir=build/tool-search-matrix/2026-07-13-post-fix-baseline` with the enforced `never` pull strategy. The paired 60-row run completed successfully in 3m57s with no trace-integrity failures.
- Generated one immutable raw comparison JSON, `manifest.json`, and `SUMMARY.md` under the ignored dated directory. The raw file is `20260714T033810.635532000Z-733d578f-tool-calling-comparison.json`, SHA-256 `bc40d88efce96871e51eb1079102184d29641b0fb0c685e63ba504b4e702ea3e`.
- The manifest records code baseline `1056b82ee5676561a7e074ca8b56427f44448f6a`, Spring AI `2.0.0`, the dirty working-tree flag for the new uncommitted automation, the complete protocol, canonical expectation fingerprint, and artifact hash.

Pass rates compare the new run with the corrected July 12 analysis; the recorded July 12 values are retained in the generated Markdown report:

| Model | Advisor | July 12 corrected | Post-fix | Delta |
| --- | --- | ---: | ---: | ---: |
| `gemma4:e2b` | standard | 8/10 | 8/10 | 0 |
| `gemma4:e2b` | Tool Search | 4/10 | 3/10 | -1 |
| `granite4.1:3b` | standard | 10/10 | 10/10 | 0 |
| `granite4.1:3b` | Tool Search | 2/10 | 2/10 | 0 |
| `qwen3.5:0.8b` | standard | 10/10 | 10/10 | 0 |
| `qwen3.5:0.8b` | Tool Search | 6/10 | 6/10 | 0 |

The post-fix Tool Search failure classification was:

| Model | No search | Zero discovery | Incomplete discovery | Discovered not executed | Execution | Output contract |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `gemma4:e2b` | 4 | 2 | 0 | 0 | 0 | 1 |
| `granite4.1:3b` | 6 | 2 | 0 | 0 | 0 | 0 |
| `qwen3.5:0.8b` | 0 | 0 | 0 | 1 | 0 | 3 |

All standard failures were output-contract misses: two `gemma4:e2b` catalog-lookup rows. The single Tool Search pass-rate delta was also `gemma4:e2b`: one catalog-lookup repetition discovered and executed the required callback but omitted `Policy FAQ` from the final output. `qwen3.5:0.8b` discovered both multi-step tools in both repetitions; one repetition did not execute them, while the other executed both but missed the required final-output term.

The comparison is observational. The post-fix run uses canonical Java request construction, while the July 12 raw request transcribed the deterministic marker incorrectly; Issue #20 also changes object-response normalization. Those are explicit confounders, so the delta is not attributed solely to model behavior or the fixes. Issue #21 affects chat no-result correctness and is not treated as a direct tool pass-rate cause.
