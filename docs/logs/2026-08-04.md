# 2026-08-04

## Phase 2 chat start gate

- Rechecked the completed Phase 1 record and confirmed the worktree was clean
  at `724436bc5f08` before the gate decision.
- Reverified ignored saved run
  `build/evaluation-matrix/2026-08-03-gemma4-e2b-a5` with the standalone
  `localEvaluationVerify` task. Verification completed offline without
  starting Spring, contacting Ollama, reanalyzing the summary, or changing the
  immutable evidence.
- Recorded the project owner's explicit selection of chat as the Phase 2 reuse
  and later portability surface. The start gate is closed and Slice S1 is
  active.
- Created `feature/chat-evidence-reuse` from merged `develop` baseline
  `2b47c07`. Phase 2 remains local, no-pull, provider-free, and offline by
  default. This decision does not authorize Anthropic credentials or calls,
  automatic model pulls, Docker, spending, a release, a tag, a push, or an
  untested migration of the existing chat endpoint.
- Updated the ignored consolidated plan plus the tracked agent guidance,
  README, deferred-work index, environment guide, test plan, and changelog so
  the active boundary and remaining authorization gates agree.

### Start-gate verification

- `./gradlew :setaccio-lab:localEvaluationVerify
  --run-dir=build/evaluation-matrix/2026-08-03-gemma4-e2b-a5 --no-daemon`
  completed successfully from the clean pre-gate checkout.
- `git diff --check` passed for the tracked gate-closure changes.
- Every relative Markdown link in the changed tracked documentation resolved
  to an existing file.
- Confirmed `plan/august-october-2026-consolidated.md` remains ignored and no
  generated evidence or build output is included in the tracked change.

## Phase 2 Slice S1: proven shared saved-evidence operations

- Audited vision, Tool Search, and local-evaluation evidence implementations.
  Their non-overwriting writes, artifact/path verification, saved-run layout
  inspection, deterministic-summary checks, and atomic summary replacement had
  compatible behavior and three real consumers.
- Added plain-Java `EvidenceFiles` for those operations and refactored all three
  suite-specific evidence implementations to use it. Existing manifests, raw
  result records, deterministic summaries, analyzer rules, failure taxonomies,
  legacy Tool Search compatibility, and hash roles remain unchanged.
- Kept provider/model, prompt/input, and invocation-outcome schemas
  suite-specific after the audit found different field names, cardinalities,
  and success/failure meanings. Slice S1 does not add a speculative generic
  suite or move Spring behavior into `setaccio-core`; Slice S2 will define only
  the narrow chat invocation contract needed by the selected surface.
- Added direct provider-free tests for non-overwriting artifact writes,
  artifact verification, deterministic text verification/replacement, saved
  layout rejection, and summary descriptors.

### Slice S1 verification

- `./gradlew :setaccio-lab:test :setaccio-lab:visionMatrixTest
  :setaccio-lab:localEvaluationTest :setaccio-lab:toolSearchSmokeTest
  --no-daemon` completed successfully. All behavior was exercised with mocks,
  synthetic results, and offline fixtures; no provider call or model pull ran.
- `./gradlew :setaccio-lab:localEvaluationVerify
  --run-dir=build/evaluation-matrix/2026-08-03-gemma4-e2b-a5 --no-daemon`
  verified the preserved Phase 1 evidence successfully through the refactored
  implementation without reanalysis or mutation.
- `git diff --check` passed, and every relative Markdown link in the changed
  tracked documentation resolved to an existing file.
- Confirmed the ignored consolidated plan remains outside Git and no generated
  evidence or build output is included in the Slice S1 change.

## Phase 2 Slice S2: minimal provider-neutral chat invocation boundary

- Added a small internal chat contract for provider and requested/effective
  model identity, prompts, common generation settings, explicit provider option
  support, and recorded response/usage/latency/attempt/failure outcomes.
- Kept Ollama-only semantics on `OllamaChatModelIdentity`: it requires a full
  lowercase SHA-256 model digest, while the generic identity does not claim
  that every provider exposes a digest. The generic settings likewise allow a
  provider to mark seed unsupported rather than inventing one.
- Added the Ollama adapter with explicit per-request model, temperature, seed,
  and output-token settings. It records a completed empty response separately
  from provider failure and classifies unavailable-model, timeout, and other
  provider failures.
- Added shared Ollama model construction that requires a loopback endpoint and
  enforces pull strategy `never`, no additional models, zero retries, and one
  attempt. The existing fact-check factory now consumes the same safety
  primitive without changing its public contract.
- Left `ChatBenchmarkService`, `/api/lab/chat`, its concurrent/cache behavior,
  and its saved result shape unchanged. Slice S3 will apply the boundary first
  to a new dedicated sequential matrix.

### Slice S2 verification

- Provider-free targeted tests covered the new contract, Ollama adapter and
  model factory, plus the existing fact-check model factory. No live provider
  call or model pull ran.
- `./gradlew :setaccio-lab:build :setaccio-lab:localEvaluationTest --no-daemon`
  completed successfully, covering the unchanged interactive chat tests and
  the fact-check runner regression suite without a live provider call.

## Phase 2 Slice S3: evidence-backed Ollama chat matrix implementation

- Promoted the exact `concise-summary`, `classification-policy`, and
  `json-shape` interactive default prompt texts into one tracked v1 matrix
  catalog. Locked its exact bytes, stable order, catalog SHA-256, per-prompt
  SHA-256 identities, and parity with the unchanged interactive defaults.
- Added a dedicated `chatMatrix` source set and opt-in task. Preflight requires
  an explicit loopback URL, already-installed model tag, positive output-token
  limit, bounded timeout, and new dated directory under `build/chat-matrix/`;
  resolves the full model digest before allocation; and never pulls a model.
- Added a locked six-row schedule: three prompts in catalog order for seed `42`,
  then the same three prompts for seed `43`, strictly sequentially with
  temperature `0.0` and one attempt. Every row uses the S2 invocation boundary;
  classified failures remain in place without retry or replacement.
- Added suite-specific raw JSON, a shared-v1 manifest, deterministic aggregate
  summary, and standalone `chatMatrixVerify`/`chatMatrixReanalyze` tasks.
  Offline analysis rejects protocol, catalog, model, schedule, row, setting,
  usage, artifact, and layout drift without making semantic quality claims.
- Kept `ChatBenchmarkService`, `/api/lab/chat`, cache/concurrency behavior, and
  the interactive saved-result format unchanged. No Anthropic path, credential,
  provider fallback, Spring startup, default-lifecycle live call, or model pull
  was added.
- No controlled Ollama matrix was run. S3 and Phase 2 remain open until an
  explicitly authorized run produces evidence that verifies and reanalyzes
  offline.

### Slice S3 implementation verification

- `./gradlew :setaccio-lab:chatMatrixTest :setaccio-lab:test --tests
  'com.setaccio.lab.chat.*'` completed successfully with mocks and synthetic
  evidence only; no provider call or model pull ran.
- `./gradlew :setaccio-lab:build :setaccio-lab:chatMatrixTest
  :setaccio-lab:localEvaluationTest --no-daemon` completed successfully. The
  ordinary build did not execute `chatMatrix`; the dedicated suite and the
  fact-check regression suite remained provider-free.
- `./gradlew help --task :setaccio-lab:chatMatrix --no-daemon` confirmed that
  the live task exposes only the five documented explicit options and states
  its no-pull, non-default-lifecycle boundary.
- `./gradlew :setaccio-core:test :setaccio-lab:test
  :setaccio-lab:chatMatrixTest :setaccio-lab:visionMatrixTest
  :setaccio-lab:toolSearchSmokeTest :setaccio-lab:localEvaluationTest
  :setaccio-testcontainers:test :setaccio-core:build :setaccio-lab:build
  :setaccio-testcontainers:build --no-daemon` completed successfully. The live
  `chatMatrix` task did not run.
