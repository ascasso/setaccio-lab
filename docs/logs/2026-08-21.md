# 2026-08-21

## Phase 2 T2.1 tracked prompt catalog

- Started the provider-free first slice of the controlled system-prompt intervention.
- Added the locked v1 catalog with exactly two ordered conditions: the existing empty untreated baseline and the versioned tool-discipline prompt.
- Bound the catalog's exact resource bytes and each prompt's exact UTF-8 text to SHA-256 identities, with tests that reject byte, text, identity, and order drift.
- This slice adds no paired runner, model invocation, provider request, model pull, or Phase 2 evidence run.

### Verification

- `./gradlew :setaccio-lab:toolCompatibilityTest` completed successfully.
- `./gradlew :setaccio-lab:build :setaccio-lab:toolCompatibilityTest` completed successfully.
- `git diff --check` passed.

## Phase 2 T2.2 paired execution protocol

- Added the isolated opt-in `toolCompatibilityPromptMatrix` task. It accepts the two fresh
  condition output directories but offers no prompt-selection option and is outside the default
  lifecycle.
- Locked one shared v1 paired-execution schedule for 32 case-major attempts: untreated then
  prompted for seed 42, then prompted then untreated for seed 43. Each condition keeps its
  canonical 16-row order and records its global paired sequence and first/second position.
- Added dual-output preflight for the locked model, options, prompt catalog, semantic oracle,
  tool definitions, shared schedule, full installed digest, and one clean Git commit before
  allocation or provider execution.
- The runner rechecks the clean commit before every logical row and stages both condition runs
  before either manifest finalizes. Repository drift invalidates any staged manifest so both
  allocated directories remain incomplete and cannot later verify or be reused as a pair.
- Each condition has its own raw JSON, deterministic summary, and shared-v1 manifest. Phase 1's
  `toolCompatibilityMatrix` CLI and evidence schema remain unchanged.
- No Ollama request, model pull, provider call, or live evidence allocation was performed.

### Verification

- `./gradlew :setaccio-lab:toolCompatibilityTest` completed successfully.
- `./gradlew :setaccio-lab:build :setaccio-lab:toolCompatibilityTest` completed successfully.
- `./gradlew :setaccio-lab:help --task toolCompatibilityPromptMatrix` completed successfully.
- `./gradlew :setaccio-lab:toolCompatibilityPromptMatrix --configuration-cache --dry-run` completed successfully without invoking the task action.
- `./gradlew :setaccio-lab:build --dry-run` confirmed that the opt-in paired task is not in the default build lifecycle.
- `git diff --check` passed.
