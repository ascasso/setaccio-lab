# 2026-08-21

## Phase 2 T2.1 tracked prompt catalog

- Started the provider-free first slice of the controlled system-prompt intervention.
- Added the locked v1 catalog with exactly two ordered conditions: the existing empty untreated baseline and the versioned tool-discipline prompt.
- Bound the catalog's exact resource bytes and each prompt's exact UTF-8 text to SHA-256 identities, with tests that reject byte, text, identity, and order drift.
- This slice adds no paired runner, model invocation, provider request, model pull, or Phase 2 evidence run.

### Verification

- `./gradlew :setaccio-lab:toolCompatibilityTest` completed successfully.
- `./gradlew :setaccio-lab:build :setaccio-lab:toolCompatibilityTest` completed successfully.
- `git diff --check` passed.

## Phase 2 T2.2 paired execution protocol

- Added the isolated opt-in `toolCompatibilityPromptMatrix` task. It accepts the two fresh
  condition output directories but offers no prompt-selection option and is outside the default
  lifecycle.
- Locked one shared v1 paired-execution schedule for 32 case-major attempts: untreated then
  prompted for seed 42, then prompted then untreated for seed 43. Each condition keeps its
  canonical 16-row order and records its global paired sequence and first/second position.
- Added dual-output preflight for the locked model, options, prompt catalog, semantic oracle,
  tool definitions, shared schedule, full installed digest, and one clean Git commit before
  allocation or provider execution.
- The runner rechecks the clean commit before every logical row and stages both condition runs
  before either manifest finalizes. Repository drift invalidates any staged manifest so both
  allocated directories remain incomplete and cannot later verify or be reused as a pair.
- Each condition has its own raw JSON, deterministic summary, and shared-v1 manifest. Phase 1's
  `toolCompatibilityMatrix` CLI and evidence schema remain unchanged.
- No Ollama request, model pull, provider call, or live evidence allocation was performed.

### Verification

- `./gradlew :setaccio-lab:toolCompatibilityTest` completed successfully.
- `./gradlew :setaccio-lab:build :setaccio-lab:toolCompatibilityTest` completed successfully.
- `./gradlew :setaccio-lab:help --task toolCompatibilityPromptMatrix` completed successfully.
- `./gradlew :setaccio-lab:toolCompatibilityPromptMatrix --configuration-cache --dry-run` completed successfully without invoking the task action.
- `./gradlew :setaccio-lab:build --dry-run` confirmed that the opt-in paired task is not in the default build lifecycle.
- `git diff --check` passed.

## Phase 2 T2.3 comparison gate

- Added the standalone, offline-only `toolCompatibilityCompare` task. It accepts one saved
  untreated run and one saved prompted run directly under `build/tool-compatibility/`; it starts
  neither Spring nor a model provider.
- The gate verifies both condition directories and their deterministic summaries before comparing
  them. It requires one clean shared Git commit, matching Spring Boot/Spring AI versions, model
  identity and digest, all non-prompt protocol and semantic-oracle identities, and the identical
  locked paired-execution schedule.
- It treats `globalPairSequence` and `conditionExecutionPosition` as schedule-derived values:
  their expected untreated/prompted differences are accepted only when each row matches the shared
  schedule for its case and repetition. Missing, equal, swapped, or inconsistent positions fail
  before any later report can render deltas.
- Observed provider-turn, tool-call, callback, output, usage, latency, and classified diagnostic
  differences remain intentionally uninterpreted here for the later deterministic-report slice.
- No Ollama request, model pull, provider call, live evidence allocation, or semantic prompt
  decision was performed.

### Verification

- `./gradlew :setaccio-lab:toolCompatibilityTest` completed successfully.
- `./gradlew :setaccio-lab:build :setaccio-lab:toolCompatibilityTest` completed successfully.
- `./gradlew :setaccio-lab:help --task toolCompatibilityCompare` completed successfully.
- `./gradlew :setaccio-lab:toolCompatibilityCompare --configuration-cache --dry-run` completed
  successfully without invoking the task action.
- `./gradlew :setaccio-lab:build --dry-run` confirmed that the opt-in comparison task is not in
  the default build lifecycle.

## Phase 2 T2.4 deterministic paired comparison report

- Extended the already gated, standalone offline `toolCompatibilityCompare` task to print a
  deterministic Markdown report only after both saved conditions pass T2.3 verification and
  parity checks.
- The report is ordered by locked case ID then repetition and records contract, required/forbidden
  tool-selection, exact-call, per-call semantic-argument, final-response, and visible-reasoning
  transitions without rendering raw model output or provider error detail.
- Its second table records provider-turn counts and later-turn failures, each observed per-turn and
  row-aggregate output-limit state, aggregate completion-token deltas, and row-latency deltas.
- Added a golden Markdown fixture with provider-free synthetic rows covering reversal and
  unchanged cases, selection changes, semantic mismatches, later-turn failure, output-limit, token,
  and latency deltas. An incomplete candidate directory is rejected before a report can render.
- The report explicitly makes no overall-winner, aggregate-score, prompt-adoption, or human
  interpretation claim. No Ollama request, model pull, provider call, live evidence allocation, or
  prompt decision was performed.

### Verification

- `./gradlew :setaccio-lab:toolCompatibilityTest` completed successfully.
- `./gradlew :setaccio-lab:build :setaccio-lab:toolCompatibilityTest` completed successfully.
- `./gradlew :setaccio-lab:help --task toolCompatibilityCompare` completed successfully.
- `./gradlew :setaccio-lab:toolCompatibilityCompare --configuration-cache --dry-run` completed
  successfully without invoking the task action.
- `./gradlew :setaccio-lab:build --dry-run` confirmed that the opt-in comparison task is not in
  the default build lifecycle.
- `git diff --check` passed.

## Phase 2 paired live execution and deterministic closeout

- From clean commit `80bc122514938e746cef7ed2c75b921b3a7876f9`, the separately
  authorized `toolCompatibilityPromptMatrix` command executed one locked
  interleaved 32-attempt run for the untreated and prompted conditions.
- The installed LFM2.5 model resolved to digest
  `2c88e114a368b8500aabb7cf32e8a16c274d2265b640c601198a784a559bc5ed`.
  Both conditions used temperature `0.0`, seed repetitions `42`/`43`, `512`
  output tokens per provider turn, `PT2M`, one attempt, no pull, and the
  standard `ToolCallingAdvisor`.
- The ignored directories
  `build/tool-compatibility/2026-08-21-lfm-prompt-untreated` and
  `build/tool-compatibility/2026-08-21-lfm-prompted` each contain 16 rows.
  Both verified and reanalyzed offline; strict paired comparison also passed.
  All 32 rows reached the same first `PROVIDER_FAILURE` boundary, with no
  observed tool calls or final responses in either condition.
- The offline verifier was repaired to recognize prompt-matrix evidence before
  this verification pass; the regression was covered by provider-free tests
  and committed in `f2bbea2`. No evidence was rerun or replaced.
- The ignored T2.5 worksheet is prepared but remains blank. No prompt-effect,
  quality, reliability, ranking, or Phase 3 authorization claim is made until
  the project owner records the human decision.

### Verification

- `./gradlew :setaccio-lab:toolCompatibilityTest --rerun-tasks` passed.
- `./gradlew :setaccio-lab:toolCompatibilityVerify --run-dir=...` passed for
  both saved conditions.
- `./gradlew :setaccio-lab:toolCompatibilityReanalyze --run-dir=...` passed
  for both saved conditions.
- `./gradlew :setaccio-lab:toolCompatibilityCompare --baseline-run=... --candidate-run=...` passed.
- `git diff --check` passed.
