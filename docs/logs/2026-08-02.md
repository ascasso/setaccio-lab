# 2026-08-02

## August Gate 0 evidence-loss closure

- Started the August local fact-checking cycle on
  `feature/local-fact-checking-evaluation` from the current `develop` merge
  baseline `fcb0d02`.
- Confirmed Spring Boot `4.1.0` and Spring AI `2.0.0` remain pinned, the
  configured Ollama endpoint is loopback, and the planned end-of-July commit is
  present in the branch history.
- Inspected the installed Ollama inventory without pulling or invoking a
  model. The existing `gemma4:e2b` candidate resolved to full digest
  `7fbdbf8f5e45a75bb122155ed546e765b4d9c53a1285f62fd9f506baa1c5a47e`.
- Confirmed that the ignored v1 and v2 controlled-run directories required by
  `visionHumanReviewPrepare` are unavailable. A read-only recovery search found
  no copy in the workspace, Trash, common temporary or user-file locations,
  Spotlight, or a configured Time Machine destination or local snapshot.
  Because those runs were ignored build output, Git cannot restore them.
- Recorded the project owner's decision to proceed without recreating or
  replacing the original runs. This closes the Prompt v1/v2 portion of Gate 0
  through an evidence-loss waiver, not a completed semantic review. No
  actual-human `adopt` / `revise` / `reject` judgment is claimed; prior semantic
  observations remain agent-assisted, Prompt v1 remains the operational
  interactive default, and Prompt v2 remains experimental and unadopted.
- Required any future Prompt v2 decision to use a separately authorized paired
  protocol, new run names, preserved evidence, and actual human review. The
  missing artifacts must not be recreated under their original names or
  represented as the original immutable evidence.
- Updated the ignored consolidated August-October plan with the same bounded
  waiver. The plan remains local-only and is not part of this commit.

### Verification

- Ran the complete offline Gate 0 baseline:

  ```text
  ./gradlew :setaccio-core:test :setaccio-lab:test \
    :setaccio-lab:visionMatrixTest \
    :setaccio-lab:toolSearchSmokeTest \
    :setaccio-testcontainers:test \
    :setaccio-core:build :setaccio-lab:build \
    :setaccio-testcontainers:build --rerun-tasks
  ```

  All 26 tasks completed successfully in 13 seconds. Default verification used
  mocks and offline fixtures; no Ollama model call, model pull, remote-provider
  request, or Docker/Testcontainers runtime occurred.
- Ran `git diff --check`.
- Restored an unrelated line-ending-only `gradlew.bat` working-copy drift to
  the tracked version before staging, so the branch closed Gate 0 from a clean
  baseline.

## Slice A1: fact-check prompt and fixture contract

- Fast-forwarded `feature/local-fact-checking-evaluation` to the exact
  `origin/develop` merge baseline `0ace695` before implementation. That upstream
  change was only the merge of this branch's Gate 0 documentation commit.
- Added tracked prompt `local-fact-check`, version `1`, with exactly one
  `{document}` and `{claim}` placeholder, an explicit single-word `yes` / `no`
  output contract, no outside knowledge, and raw-byte SHA-256
  `e75e0ddd9bef80eecf27e1b668cef954a5eddb5a74b5e4c19db97710c3d39470`.
- Added catalog `local-fact-check-fixtures`, version `1`, with three short
  repository-authored documents. Each document has one supported and one
  unsupported claim, for six stable fixtures and a balanced 3/3 label cohort.
  The catalog raw-byte SHA-256 is
  `077d63fe5af596454127babf809075ebc61857cb5e1694c4fae1e58c0d844dac`.
- Presented the exact six document/claim/expected-verdict combinations to the
  project owner. The project owner confirmed that all six expected verdicts are
  correct.
- Added tracked review `local-fact-check-fixture-review`, version `1`, with
  status `confirmed`, date `2026-08-02`, the exact catalog identity/digest, and
  all six fixture IDs in catalog order. Its raw-byte SHA-256 is
  `55a5c452dd58a6dddf9d9012cdfb68e50a127226fd49abfaa30597d5e8310161`.
- Added dedicated catalog and review validation. The contract requires stable
  safe IDs, non-blank content, exact count/order/balance/pair structure, matching
  document text within each pair, opposite expected verdicts, and complete
  human confirmation. Pending status, incomplete IDs, and catalog-digest drift
  fail offline.
- Kept the deterministic `POST /api/lab/evaluations` behavior unchanged. This
  slice adds no `FactCheckingEvaluator` invocation, live judge model, runner,
  evidence output, dependency, credential, provider call, model pull, Docker,
  or Testcontainers runtime behavior.

### Slice A1 verification

- Ran
  `./gradlew :setaccio-lab:test --tests com.setaccio.lab.evaluation.LocalFactCheckContractTest`.
  The focused prompt, catalog, fixture, and human-review contract tests passed.
- The first full verification run exposed a Spring application-context failure:
  `LocalFactCheckFixtureReview` has a package-private test constructor in
  addition to its public dependency-injection constructor, so Spring required
  the intended constructor to be explicit. Added `@Autowired` to that public
  constructor and retained the package-private constructor for negative tests.
- Re-ran
  `./gradlew :setaccio-lab:test :setaccio-lab:visionMatrixTest :setaccio-lab:toolSearchSmokeTest :setaccio-core:build :setaccio-lab:build :setaccio-testcontainers:build --rerun-tasks`.
  All 26 tasks completed successfully in 10 seconds. Verification remained
  offline: no Ollama model call, model pull, remote-provider request, or
  Docker/Testcontainers runtime occurred.
- Ran `git diff --check` after the final documentation update.

## Gradle wrapper tracking fix

- Fixed Gradle wrapper execution (`./gradlew`) failing after git operations due to `gradle/wrapper/gradle-wrapper.jar` being ignored by `.gitignore` (`*.jar`).
- Added an exception rule `!gradle/wrapper/gradle-wrapper.jar` to `.gitignore`.
- Regenerated `gradle/wrapper/gradle-wrapper.jar` via `gradle wrapper`.
- Verified `./gradlew test` completes successfully.

