# 2026-08-02

## August Gate 0 evidence-loss closure

- Started the August local fact-checking cycle on
  `feature/local-fact-checking-evaluation` from the current `develop` merge
  baseline `fcb0d02`.
- Confirmed Spring Boot `4.1.0` and Spring AI `2.0.0` remain pinned, the
  configured Ollama endpoint is loopback, and the planned end-of-July commit is
  present in the branch history.
- Inspected the installed Ollama inventory without pulling or invoking a
  model. The existing `gemma4:e2b` candidate resolved to full digest
  `7fbdbf8f5e45a75bb122155ed546e765b4d9c53a1285f62fd9f506baa1c5a47e`.
- Confirmed that the ignored v1 and v2 controlled-run directories required by
  `visionHumanReviewPrepare` are unavailable. A read-only recovery search found
  no copy in the workspace, Trash, common temporary or user-file locations,
  Spotlight, or a configured Time Machine destination or local snapshot.
  Because those runs were ignored build output, Git cannot restore them.
- Recorded the project owner's decision to proceed without recreating or
  replacing the original runs. This closes the Prompt v1/v2 portion of Gate 0
  through an evidence-loss waiver, not a completed semantic review. No
  actual-human `adopt` / `revise` / `reject` judgment is claimed; prior semantic
  observations remain agent-assisted, Prompt v1 remains the operational
  interactive default, and Prompt v2 remains experimental and unadopted.
- Required any future Prompt v2 decision to use a separately authorized paired
  protocol, new run names, preserved evidence, and actual human review. The
  missing artifacts must not be recreated under their original names or
  represented as the original immutable evidence.
- Updated the ignored consolidated August-October plan with the same bounded
  waiver. The plan remains local-only and is not part of this commit.

### Verification

- Ran the complete offline Gate 0 baseline:

  ```text
  ./gradlew :setaccio-core:test :setaccio-lab:test \
    :setaccio-lab:visionMatrixTest \
    :setaccio-lab:toolSearchSmokeTest \
    :setaccio-testcontainers:test \
    :setaccio-core:build :setaccio-lab:build \
    :setaccio-testcontainers:build --rerun-tasks
  ```

  All 26 tasks completed successfully in 13 seconds. Default verification used
  mocks and offline fixtures; no Ollama model call, model pull, remote-provider
  request, or Docker/Testcontainers runtime occurred.
- Ran `git diff --check`.
- Restored an unrelated line-ending-only `gradlew.bat` working-copy drift to
  the tracked version before staging, so the branch closed Gate 0 from a clean
  baseline.
