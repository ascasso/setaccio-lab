# 2026-08-17

## T0.1 small-model tool-compatibility documentation packet

- Completed the T0.1 cross-document alignment for the locked untreated LFM2.5
  Phase 1 protocol: the standard `ToolCallingAdvisor`, eight ordered canonical
  cases, two seeded repetitions, `512` output tokens per provider turn, one
  `PT2M` whole-row deadline, one logical attempt with ordered provider turns,
  no pull, 16 sequential rows, the versioned semantic call oracle, dedicated
  source sets/tasks, and standalone offline verification/reanalysis.
- Recorded the documentation packet completion in the plan, repository guide,
  deferred-work index, test plan, environment guide, and changelog.
- Created the dedicated `feature/tool-compatibility-plan` branch named by the
  plan's branch strategy before changing documentation.

### Authorization boundary

- Documentation only. T0.1 does not authorize T1.1 provider-free code, model
  inspection, model pull, Ollama or remote-provider calls, credentials, Docker,
  ignored evidence, push, release, or tag.
- The provider-free implementation gate remains an explicit project-owner
  instruction for T1.1. Any live matrix remains a later separately reviewed
  exact command after provider-free implementation and tests pass from a clean
  commit.

### Verification

- Every relative Markdown link in all 41 tracked Markdown files resolved to an
  existing repository path.
- Markdown fences are balanced in every changed Markdown file.
- No stale `SmallModelToolCallingCompatibilityPlan.md` filename reference was
  found.
- `git diff --check` passed, followed by final worktree review before commit.

## T1.1 provider-free protocol foundation

- Began the separately authorized T1.1 implementation on the dedicated
  `feature/tool-compatibility-plan` branch.
- Added only the planned `toolCompatibility` and `toolCompatibilityTest` source
  sets, immutable suite-specific protocol/value types, the exact tracked
  `tool-case-oracle` resource, and provider-free protocol tests.
- Added no runner, live matrix task, provider call, model inspection, model
  pull, credential access, Docker use, or ignored evidence.

### Verification

- `bash ./gradlew :setaccio-lab:toolCompatibilityTest :setaccio-lab:test
  --no-daemon` completed successfully without a provider call.
- The tracked oracle resource hashes to the locked SHA-256
  `9ccd612ace107b37a6f5a5c30e0cc77e80236a54afcb728566750c18035c1af4`.
- `:setaccio-lab:tasks --all` lists only the provider-free
  `toolCompatibilityTest` task and supporting classes; no matrix, verification,
  reanalysis, or comparison task exists yet.
- `git diff --check` passed, followed by final worktree review before commit.

### Open T1.1 review notes

- The canonical case/tool fingerprints are currently computed from the live
  fixtures rather than checked against locked expected values, so prompt,
  expectation, or same-name tool-definition drift is not yet rejected.
- The repository status documents still describe the pre-T1.1 state. Their
  packet-path conflict remains to be resolved separately; neither finding is
  changed as part of T1.2.

## T1.2 explicit untreated system-prompt identity

- Added the exact `tool-system-none` version 1 identity with empty UTF-8 text,
  `present=false`, and the SHA-256 of zero bytes.
- Added locked-baseline validation, constructor invariant coverage, exact JSON
  round-trip coverage, and drift tests for ID, version, text, presence, and
  digest semantics.
- Added no Phase 2 prompt, model inspection, invocation boundary, runner, live
  task, provider call, credential access, Docker use, or ignored evidence.

### Verification

- `bash ./gradlew :setaccio-lab:toolCompatibilityTest --no-daemon` completed
  successfully without a provider call.
- The tests confirm the locked SHA-256 is the digest of zero bytes rather than
  a newline or sentinel, and the exact identity survives JSON round-trip.
- `git diff --check` passed, followed by final worktree review before commit.

## T1.3 standard-advisor invocation and observability boundary

- Added a suite-local one-row invocation boundary that uses the standard Spring
  AI `ToolCallingAdvisor`, never Tool Search or a custom lower-level tool loop.
  Its temporary in-memory trace keeps each provider turn distinct from each
  tool callback and is not a final result-row or evidence schema.
- Added provider-free proof coverage for the initial tool request, a second
  tool request after the first callback, and the final assistant completion.
  The trace preserves nullable and blank assistant text, finish and response
  metadata, available usage, output-limit state, latency, raw tool arguments,
  declared-schema observations, semantic-oracle agreement, callback outcomes,
  and per-turn/provider failures without flattening recursive turns.
- Added the bounded raw JSON-schema validator for the current public fixture
  definitions. It accepts only the locked subset and metadata keywords and
  rejects unsupported constraints rather than adding a schema dependency or
  approximating validation. Raw schema validity remains separate from Spring
  AI callback coercion and callback success.
- Added provider-free callback binding and invocation failure cases, later-turn
  provider failure retention, a row-timeout/no-overlap proof, and the hard stop
  that refuses another sequential row when cancelled work does not stop within
  the confirmation interval.
- Added no-pull, one-attempt Ollama model construction through the existing
  factory with the locked loopback, model, temperature, seed, token, and
  `PT2M` settings. The test constructs it only; it does not inspect a model or
  contact Ollama. Installed-model/digest preflight, live task wiring, and
  evidence lifecycle remain later slices.

### Verification

- `bash ./gradlew :setaccio-lab:toolCompatibilityTest --no-daemon` completed
  successfully with only fake `ChatModel` implementations and deterministic
  fixture callbacks.
- `bash ./gradlew :setaccio-lab:toolCompatibilityTest :setaccio-lab:test
  --no-daemon` completed successfully without a provider call.
- No live matrix task, model inspection or pull, credential access, Docker use,
  ignored evidence, final result-row schema, or change to the existing
  `RecordingToolCallAdvisor` was added.

## Phase 2 paired-schedule comparator correction

- Corrected the proposed `toolCompatibilityCompare` contract after review: the
  paired baseline and candidate rows necessarily carry different
  `globalPairSequence` and `conditionExecutionPosition` values.
- The comparator must resolve each paired row against the identical shared
  paired-execution schedule. It accepts position differences only when they
  match that schedule and rejects missing, swapped, equal, or otherwise
  schedule-inconsistent values.
- Added the corresponding provider-free comparison-test requirement. The shared
  schedule identity remains a strict parity requirement.

### Authorization boundary

- Documentation changes only. No source set, Gradle task, implementation class,
  test, model inspection, provider call, credential access, Docker use, or
  ignored evidence was added or used.

### Verification

- `git diff --check` passed.
- Focused Markdown link and fence checks passed.

## Phase 3 Qwen3.8 reference refresh

- Revised the provisional Phase 3 cohort in
  [SmallModelToolCallingCompatibilityPlan.md](../SmallModelToolCallingCompatibilityPlan.md)
  from the project owner's reported installed-model list without changing the
  locked LFM2.5 Phase 1 or Phase 2 protocol.
- Nominated the exact installed `qwen3.8:27b-mlx` tag as the provisional
  higher-capability reference candidate, subject to later T3.1 approval and
  full-digest locking. Removed the absent `dolphin-phi:latest` peer rather than
  pulling it or choosing an unreviewed replacement.
- Required the future cohort preflight and evidence to retain model-family
  provenance, artifact/runtime format, quantization or precision, template and
  tool metadata, thinking-mode metadata where exposed, and one recorded Ollama
  server/runtime version. Unknown optional metadata remains explicitly
  unavailable rather than inferred.
- Clarified that MLX-versus-non-MLX latency and token-use observations describe
  exact deployed artifacts under one recorded runtime, not weight-only or
  backend-normalized model performance.
- Removed the brittle six-model wording from the optional capability-frontier
  claim.

### Authorization boundary

- Documentation changes only. No model inspection, model pull, Ollama call,
  implementation, provider call, credential access, Docker use, or ignored
  evidence was added or used.
- The reference and cohort remain provisional until T3.1 and later live work
  receive their separate explicit authorizations.

### Verification

- `git diff --check` passed.
- Focused Markdown link, fence, and stale-reference checks passed.

## T1.3 review corrections

- Changed an unconfirmed timeout cancellation from a returned compatibility
  trace into an immediate experimental-integrity exception. The exception
  retains the completed partial trace, marks it unsafe for another sequential
  attempt, and the boundary remains permanently closed to later attempts.
- Kept suite protocol-integrity exceptions distinct from provider failures.
  Broken observability/linkage or programmer-invariant failures now escape the
  row boundary, while an unresolvable model-selected tool is retained as an
  ordinary callback-resolution compatibility failure.
- Classified method callback binding versus invocation failures from Spring
  AI's observed typed exception/cause path rather than inferring binding from
  raw declared-schema validity or one fixture message. Provider-free coverage
  includes a schema-invalid numeric value that binds to a string and then
  fails during tool invocation.
- Bound controlled Ollama model construction to a suite-owned installed-model
  inventory resolution. The temporary construction result retains the locked
  requested/effective tag and full immutable digest, and rejects missing,
  incomplete, or duplicate normalized identities without contacting Ollama.
- Added no final result-row schema, runner, live task, provider call, model
  inspection or pull, credential access, Docker use, or ignored evidence.

### Verification

- `bash ./gradlew :setaccio-lab:toolCompatibilityTest --no-daemon` completed
  successfully using fake models, deterministic callbacks, and an in-memory
  installed-model inventory only.
- `bash ./gradlew :setaccio-lab:toolCompatibilityTest :setaccio-lab:test
  --rerun-tasks --no-daemon` completed successfully without a provider call.
- `git diff --check` passed before commit.
