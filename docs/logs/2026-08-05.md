# 2026-08-05

## Phase 3 Slice O1 offline implementation

- Created local branch `feature/anthropic-portability` from the clean merged
  Phase 2 closeout before beginning Phase 3 work.
- Re-checked the pinned Spring AI `2.0.0` Anthropic API against current
  official Spring AI and Anthropic documentation. Selected
  `claude-haiku-4-5-20251001`, a provider-pinned hosted model identifier; no
  immutable local digest is claimed.
- Added only the mocked Anthropic adapter behind the existing provider-neutral
  chat boundary. It maps temperature, maximum output tokens, SDK timeout, and
  one attempt through `maxRetries=0`; it records seed as unsupported and never
  simulates it. It retains only returned effective-model/usage data and a
  format-validated opaque response ID, excluding credentials, headers, and base
  URLs from outcomes.
- No Anthropic credential was read or supplied, no remote request was made,
  and no live runner was added. Slice O2 and the controlled run remain deferred
  pending a current cost calculation plus separate explicit remote-call and
  maximum-USD authorization.

### Verification

- `./gradlew :setaccio-lab:test --tests 'com.setaccio.lab.chat.*' --no-daemon`
  completed successfully with mocked chat models only.

## Phase 3 Slices O2 and O3 offline implementation

- Added the raw-output-free provider portability projection and a separately
  opt-in six-call Anthropic matrix. It keeps the versioned hosted model ID
  distinct from Ollama's local digest and compares only shared architecture
  protocol fields; it makes no answer-quality, performance, or model-ranking
  claim.
- Locked the Anthropic protocol to `claude-haiku-4-5-20251001`, the tracked
  three-prompt catalog, two unseeded repetitions, temperature `0.0`, `128`
  output tokens, `PT2M`, one attempt, and no retries (`maxRetries=0`). Common
  option treatment is explicit: temperature and output tokens direct; timeout
  and retry policy translated; seed rejected and not simulated; none ignored.
- Added ignored non-overwriting evidence under `build/anthropic-chat-matrix/`:
  raw result, raw-output-free snapshot, v1 manifest, deterministic summary,
  and standalone offline verify/reanalyze tasks. Credentials, headers, base
  URLs, account data, and raw answers are excluded from public output.
- The project owner separately authorized this fixed remote proof, first with a
  `$3.00` maximum and then an increased `$5.00` maximum. The runner retained a
  stricter `$3.00` ceiling and recalculates the official-price worst-case
  estimate immediately before an explicit invocation. No remote request is
  claimed in this implementation record.

### Verification

- `./gradlew :setaccio-lab:chatMatrixTest --tests
  'com.setaccio.lab.chatmatrix.AnthropicChatMatrixEvidenceTest' --tests
  'com.setaccio.lab.chatmatrix.AnthropicChatMatrixRunnerTest' --tests
  'com.setaccio.lab.chatmatrix.ChatPortabilityReportTest' --no-daemon`
  completed successfully without a credential or provider call.
- The first explicit O3 invocation stopped during verified-Ollama snapshot
  projection, before credential lookup, output allocation, or a remote request,
  because the local zero-cost estimate omitted the locked output-token ceiling.
  The preflight now records the six-row `768`-token ceiling at a zero local API
  price, with focused regression coverage; the failed preflight spent `$0`.
- After the six remote rows completed, the in-process evidence check passed but
  the standalone verifier found that duplicate manifest settings compared JSON
  object insertion order and numeric node width. Verification now compares the
  same JSON values order-independently with numeric equivalence. This changes no
  preserved raw row, manifest hash, response, or provider call.

## Phase 3 O3 controlled portability proof closeout

- The owner explicitly authorized a replacement six-call local Ollama baseline
  after the prior ignored Phase 2 directory was found absent. From clean commit
  `215ea182941da3fc6773c7ce9f47f76e517b753a`, installed `gemma4:e2b` resolved
  to digest
  `7fbdbf8f5e45a75bb122155ed546e765b4d9c53a1285f62fd9f506baa1c5a47e`.
  All six one-attempt, no-pull rows completed with usage metadata and empty
  responses. Its ignored replacement evidence verified and reanalyzed offline.
- Immediately before the remote run, official Anthropic pricing was refreshed
  at `$1` per million input tokens and `$5` per million output tokens, and the
  API model ID remained `claude-haiku-4-5-20251001`. The locked `1,536` input-
  token and `768` output-token ceilings produced a `$0.005376` worst-case
  estimate. The task used a `$3` ceiling within the owner's `$5` authorization.
- From clean commit `3810a194198a25eb0ae716aec86e64015f9b30c9`, exactly six
  sequential Anthropic calls ran over the tracked three-prompt catalog and two
  unseeded repetitions. All six completed with non-empty output and complete
  usage metadata; no row was retried, replaced, or selectively rerun. The
  usage-derived cost was `$0.001870`.
- Ignored evidence is preserved at
  `build/anthropic-chat-matrix/2026-08-05-claude-haiku-4-5-o3/`; the raw artifact
  SHA-256 is `180bc34e4c507041b4cf6b54ababb641a0a7411fea6b952805ad6bd2ee1db18e`.
  After the restart-stable manifest-settings comparison fix, standalone
  verification and deterministic reanalysis both completed without a provider
  call or credential read.
- Prompt inputs, shared invocation settings, and Spring framework versions
  matched the replacement Ollama baseline, so the narrow architecture contract
  is compatible. Anthropic seed remains unsupported and unsimulated, and a
  hosted versioned ID does not provide the local digest semantics of Ollama.
  No answer-quality, performance, reliability, causation, model-ranking,
  endpoint-migration, or future remote-call claim is made.

### Closeout verification

- `./gradlew :setaccio-lab:chatMatrixVerify
  --run-dir=build/chat-matrix/2026-08-05-gemma4-e2b-o3-baseline
  :setaccio-lab:chatMatrixReanalyze
  --run-dir=build/chat-matrix/2026-08-05-gemma4-e2b-o3-baseline --no-daemon`
  completed successfully without a provider call.
- `./gradlew :setaccio-lab:chatMatrixTest
  :setaccio-lab:anthropicChatMatrixVerify
  --run-dir=build/anthropic-chat-matrix/2026-08-05-claude-haiku-4-5-o3
  :setaccio-lab:anthropicChatMatrixReanalyze
  --run-dir=build/anthropic-chat-matrix/2026-08-05-claude-haiku-4-5-o3
  --rerun-tasks --no-daemon` completed successfully; the verify/reanalyze tasks
  were offline and provider-free.

## Phase 2 local chat reuse proof closeout

- Ran the explicitly authorized local-only `chatMatrix` task from clean commit
  `51025cfad3023ee80dfc866663f47b46fa159e6b` against already-installed
  `gemma4:e2b` (full digest
  `7fbdbf8f5e45a75bb122155ed546e765b4d9c53a1285f62fd9f506baa1c5a47e`).
  The run used the locked three-prompt catalog, temperature `0.0`, seeds
  `42`/`43`, `128` maximum output tokens, `PT2M`, one attempt per row, and
  pull strategy `never`.
- All six sequential invocations completed and retained complete usage
  metadata. All six returned empty responses; none was classified as model
  unavailable, timeout, or provider failure. No row was retried, replaced, or
  selectively rerun.
- Wrote the raw result, shared-v1 manifest, and deterministic summary only to
  ignored `build/chat-matrix/2026-08-05-gemma4-e2b-s3/`. The raw result SHA-256
  is `ae37d289db06ebfccd65f59661cc4a93c9c3bb3f3965208feb2a968374d20e05`.
- `chatMatrixVerify` and `chatMatrixReanalyze` both completed offline against
  that saved evidence. `localEvaluationVerify` also reconfirmed the preserved
  Phase 1 evidence offline.
- Closed Phase 2 as a shared-contract and evidence-lifecycle reuse proof. This
  run makes no chat-quality, reliability, causation, or model-ranking claim;
  the interactive endpoint remains unchanged. Phase 3 remains a separately
  authorized candidate behind its compatibility, credential, and cost gates.

### Closeout verification

- `./gradlew :setaccio-lab:chatMatrix --ollama-base-url=http://localhost:11434
  --model=gemma4:e2b --max-tokens=128 --timeout=PT2M
  --output-dir=build/chat-matrix/2026-08-05-gemma4-e2b-s3 --no-daemon`
  completed successfully.
- `./gradlew :setaccio-lab:chatMatrixVerify
  --run-dir=build/chat-matrix/2026-08-05-gemma4-e2b-s3 --no-daemon` and
  `./gradlew :setaccio-lab:chatMatrixReanalyze
  --run-dir=build/chat-matrix/2026-08-05-gemma4-e2b-s3 --no-daemon` completed
  successfully without starting Spring or calling a model provider.
- `./gradlew :setaccio-lab:localEvaluationVerify
  --run-dir=build/evaluation-matrix/2026-08-03-gemma4-e2b-a5 --no-daemon`
  completed successfully without starting Spring or calling a model provider.
- `./gradlew :setaccio-lab:build :setaccio-lab:chatMatrixTest
  :setaccio-lab:localEvaluationTest --no-daemon` completed successfully with
  the live matrix excluded from the default lifecycle.
- `git diff --check` passed; the ignored plan and generated evidence remain
  outside the tracked closeout change.

## Plan retirement and documentation audit

- The project owner deleted the ignored files under `plan/` after Phase 3
  closeout. The tracked `AGENTS.md`, `README.md`, `CHANGELOG.md`,
  `docs/DEFERRED-WORK.md`, `docs/ENVIRONMENT.md`, `docs/TEST-PLAN.md`, and
  dated logs now record Phase 1, Phase 2, and the bounded Phase 3 proof as
  complete; remaining work is limited to the explicitly deferred scope outside
  the three-phase plan.
- Historical dated logs retain their original plan-path references as audit
  history. No active tracked documentation links to the deleted plan files.
