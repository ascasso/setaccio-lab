# 2026-08-15

## Small-model compatibility plan execution hardening

- Converted
  [SmallModelToolCallingCompatibilityPlan.md](../SmallModelToolCallingCompatibilityPlan.md)
  from a research-oriented proposal into a slice-by-slice Codex execution
  runbook without implementing the benchmark.
- Locked the future Phase 1 protocol to the existing eight canonical cases and
  complete fixture tool set, one explicit LFM2.5 tag, standard
  `ToolCallingAdvisor`, two repetitions with seeds `42`/`43`, temperature
  `0.0`, `512` output tokens, `PT2M`, one attempt, no pull, repetition-then-case
  order, and exactly 16 rows.
- Locked suite-specific source sets, package, task names, raw filename, manifest
  suite/engine values, output root, exact three-artifact evidence layout, and
  the SHA-256 identity of the empty untreated system prompt.
- Added a bounded raw-argument schema-validation policy for the current fixture
  schemas. Unsupported schema shapes must stop preflight; callback DTO coercion
  remains observable evidence rather than proof of schema-valid model output.
- Preserved `no-applicable-domain-tool` unchanged and documented its inherited
  "Use tool discovery" wording as a known limitation under the standard-advisor
  study rather than silently changing an experimental input.
- Added one universal Codex dispatch contract plus an execution packet for every
  formal slice. Each packet states routing, dependencies, allowed paths,
  deliverables, checks, and stop conditions. Terra is the default executor,
  Luna is limited to marked mechanical packets, and Sol is an optional
  architecture escalation/review choice rather than a requirement for every
  slice.
- Aligned `AGENTS.md`, `CHANGELOG.md`, `docs/DEFERRED-WORK.md`,
  `docs/TEST-PLAN.md`, and `docs/ENVIRONMENT.md` with the prepared Phase 1
  contract and its still-open implementation/live-run gates.

### Authorization boundary

- Documentation hardening only. No `toolCompatibility` source set, Gradle task,
  implementation class, test, model inspection, model pull, Ollama call,
  remote-provider request, Docker use, credential access, ignored evidence,
  push, release, or tag was created or performed.
- T1.1 remains blocked on a new explicit project-owner instruction to implement
  the provider-free protocol model. Any live LFM matrix remains a later,
  separately reviewed exact command after the complete provider-free Phase 1
  implementation passes from a clean commit.

### Verification

- `git diff --check` passed.
- Every relative Markdown link in the tracked documentation plus this dated log
  resolved to an existing repository path.
- Markdown fence counts were balanced in every changed Markdown file.
- `shasum -a 256 /dev/null` confirmed the locked SHA-256 identity of the empty
  untreated system prompt.
- `./gradlew :setaccio-lab:tasks --all --no-daemon` completed successfully and
  confirmed that no `toolCompatibility` task or source set exists yet.
- No provider-backed task or live model call was run.
