# 2026-08-24

## Controlled Phase 3 tool-compatibility cohort execution

- After the owner-approved preflight, invoked
  `:setaccio-lab:toolCompatibilityCohort` exactly once against loopback
  Ollama. The clean baseline was commit
  `e897edf81cd8397abab469eb80bdb054cadbe5cd`.
- The preflight and runner lock accepted Ollama `0.32.15` and the six approved,
  distinct local model identities and full digests: five ordered peers plus the
  separately labelled `qwen3.8:27b-mlx` reference. No mutable `:latest` alias,
  remote model, duplicate-digest alias, pull, substitution, or per-model
  customization was used.
- The run used the locked untreated prompt policy, standard `ToolCallingAdvisor`,
  two sequential repetitions of each canonical case per model (96 planned
  rows), temperature `0.0`, explicit seeds `42` and `43`, `512` maximum output
  tokens per provider turn, one `PT2M` logical-row deadline, one logical
  attempt, and no thinking-mode override.
- The task completed and wrote immutable local evidence only under the ignored
  `setaccio-lab/build/tool-compatibility/2026-08-24-approved-cohort/`
  directory. No remote provider, credential, or Docker path was used.
- The saved evidence passed both standalone offline checks:
  `:setaccio-lab:toolCompatibilityCohortVerify` and
  `:setaccio-lab:toolCompatibilityCohortReanalyze`.
- This is an execution and evidence-integrity record only. It makes no model
  quality, reliability, compatibility, or ranking conclusion; evidence
  interpretation and T3.5 remain pending separate owner authorization.
