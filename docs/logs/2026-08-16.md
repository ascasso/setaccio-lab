# 2026-08-16

## Small-model compatibility protocol review hardening

- Revised
  [SmallModelToolCallingCompatibilityPlan.md](../SmallModelToolCallingCompatibilityPlan.md)
  after a focused review that intentionally excluded cosmetic rewriting and
  minor editorial changes.
- Distinguished one logical row attempt from the ordered provider turns made by
  Spring AI's recursive standard `ToolCallingAdvisor`. The future evidence must
  retain turn-level assistant, finish, usage, output-limit, latency, and failure
  state plus per-call linkage before producing row aggregates.
- Locked `512` output tokens per provider turn and one `PT2M` wall-clock deadline
  around the complete row. A timeout may remain a compatibility row only when
  provider work is confirmed stopped; otherwise the matrix aborts rather than
  allowing hidden overlap with the next sequential row.
- Added the exact versioned `tool-case-oracle` bytes and digest. Its ordered
  calls and JSON-typed argument values fill the semantic gap in the canonical
  fixtures without copying their prompts, existing expectations, or schemas.
  Raw JSON, declared-schema validity, semantic argument agreement, callback
  coercion/execution, and final-output assertions remain separate dimensions.
- Deferred the final row/result/failure/diagnostic schema from T1.1 until T1.3
  proves what the standard advisor can observe across multiple provider turns.
- Added the future `toolCompatibilityPromptMatrix` contract as Phase 2's only
  paired live interface. One separately approved invocation must preflight both
  outputs and produce the untreated and prompted 16-row runs through the locked
  32-attempt interleaved schedule; two independent whole-condition commands are
  not an acceptable substitute. It must re-check the original clean commit
  before every row and before manifest finalization, aborting both runs as
  incomplete if the repository drifts.
- Aligned `AGENTS.md`, `CHANGELOG.md`, `docs/DEFERRED-WORK.md`,
  `docs/TEST-PLAN.md`, and `docs/ENVIRONMENT.md` with those protocol decisions.

### Authorization boundary

- Documentation changes only. No `toolCompatibility` source set, resource,
  Gradle task, implementation class, or test was added.
- No model inspection, model pull, Ollama or remote-provider call, credential
  access, Docker use, ignored evidence, push, release, or tag occurred.
- T1.1 still requires a separate explicit implementation instruction, and each
  future live matrix still requires separate approval of its exact command.

### Verification

- `git diff --check` passed.
- Every relative Markdown link in all 39 tracked Markdown files plus this new
  dated log resolved to an existing repository path.
- Markdown fence counts were balanced in every changed Markdown file.
- The embedded `tool-case-oracle` parsed as JSON with eight cases and hashed to
  the locked SHA-256
  `9ccd612ace107b37a6f5a5c30e0cc77e80236a54afcb728566750c18035c1af4`.
- `./gradlew :setaccio-lab:test --tests
  com.setaccio.lab.fixture.ToolBenchmarkCasesTest --tests
  com.setaccio.lab.tool.BenchmarkToolsTest :setaccio-lab:tasks --all
  --no-daemon` completed successfully. It confirmed the canonical fixture cases
  and tool definitions still pass and that no `toolCompatibility` task or source
  set exists yet.
- No provider-backed task or live model call was run.
