# 2026-08-23

## Dependency-independent Phase 3 cohort preparation

- At the start of the day, the project owner authorized as much Phase 3
  preparation as could proceed while the owner-only T2.5 decision was still
  pending.
- Added a provider-free cohort preflight foundation in the existing
  `toolCompatibility` source set. It accepts explicit ordered peer tags and one
  separately labelled reference, resolves normalized installed identities and
  full digests from a fake/read-only inventory, requires one Ollama runtime
  version, and validates one fresh dated output path without allocating it.
- Added explicit available/unavailable metadata fields for size, model-family
  provenance, artifact/runtime format, quantization or precision, template and
  default-system-prompt fingerprints, tool capability, and thinking mode.
- Preflight rejects missing or remote models, duplicate normalized requests,
  duplicate aliases/bytes, incomplete digests, unsafe output paths, and a
  mutable `:latest` tag when a versioned alias exposes the same bytes.
- Added the deterministic T3.2 prompt-policy foundation. It maps `adopt` to the
  tracked discipline prompt, `reject` to untreated operation, `revise` to a
  blocked execution state, and `inconclusive` to untreated operation plus an
  explicit limitation. It rejects drift in the baseline/candidate run IDs,
  prompt-catalog digest, comparison-report digest, or review date through an
  exact decision binding.
- Read-only Ollama inspection reported runtime `0.32.15` and found every
  provisional peer plus `qwen3.8:27b-mlx` already installed. The observed tags
  remained provisional at this checkpoint: this work did not approve or lock
  the exact cohort/reference identities or establish model compatibility.
- This initial checkpoint invoked no model, allocated no live output directory,
  pulled no model, and added no cohort task or live evidence path. The later
  provider-free T3.3 core is recorded separately below.

## Provider-free T3.3 cohort execution and evidence core

- Added a dynamic immutable model-major schedule that binds one Ollama runtime
  version, ordered peer/reference identities, full digests, metadata, all
  sixteen case/repetition rows per model, and supported or unsupported seed
  semantics into one SHA-256 identity.
- Generalized only the canonical row-analysis seam needed by cohort results;
  the existing Phase 1/2 result contracts still require the locked LFM2.5 model
  and exact seeded rows.
- Added a provider-free cohort executor with a fresh standard-advisor boundary
  per model. It rechecks the runtime before each row, requires a fresh
  identity-bound controlled model, preserves model-major order, omits the seed
  option when explicitly unsupported, applies no thinking override, and
  retains classified provider failures without replacing rows.
- Added a strict cohort result and shared-v1 evidence lifecycle with one raw
  JSON result, one deterministic structural Markdown summary, one manifest,
  offline tamper detection, and byte-for-byte summary reanalysis. Reporting is
  per model and produces no aggregate score or winner.
- Added `toolCompatibilityCohortVerify` and
  `toolCompatibilityCohortReanalyze` as provider-free opt-in tasks outside the
  default lifecycle. No live `toolCompatibilityCohort` task was added.
- Fake-session tests cover 48 ordered rows across two peers and one reference,
  unsupported seed omission, effective-thinking metadata retention, no
  thinking override, runtime drift, first-provider-failure retention, later
  model continuation, evidence tampering, and standalone offline reanalysis.
- No model was invoked, no evidence output was allocated outside tests, and no
  model was pulled. At this checkpoint the live runner remained gated on T2.5
  and exact identity approval.

## Provider-free T3.4 multidimensional cohort analysis

- Expanded the cohort summary from its T3.3 structural projection into ordered
  per-model compatibility, discipline, argument, multi-step, failure-recovery,
  output-behavior, efficiency, and incomplete/unsupported sections.
- Added deterministic no-match handling, missing/unknown/incorrect argument,
  first/second-call, dependency-order, post-callback continuation,
  premature-final, duplicate-call, deterministic-failure retention, and empty
  failure-response observations.
- Kept error-reporting, success-claim, and response-format pollution counts
  explicitly lexical. Final-response character median/range is descriptive and
  is not a semantic concision judgment.
- Added observed prompt/completion/total token sums with exact provider-turn
  coverage. Tokens per passing row is emitted only when every passing row has
  complete aggregate usage; otherwise it remains unavailable.
- Added explicit unavailable metadata, supported/unsupported seed state, and a
  mixed artifact/runtime caveat that binds latency and token observations to
  the deployed tag, digest, artifact format, and Ollama version.
- Added successful, anomaly, complete/incomplete usage, mixed GGUF/MLX, ordered
  peer/reference, and golden report-digest fixtures. The report contains no
  winner, ranking, or leaderboard section.
- No real cohort evidence was read or written, no model was invoked, and no
  model was pulled. At this checkpoint T3.5 and live evidence application
  remained gated on T2.5, exact identity approval, and the live T3.3 boundary.

## T2.5 human interpretation closeout

- The project owner completed the private ignored worksheet on 2026-08-23 and
  selected `inconclusive`.
- The decision is bound to baseline run
  `2026-08-21-lfm-prompt-untreated`, candidate run
  `2026-08-21-lfm-prompted`, prompt-catalog digest
  `d55122cd60ac056c8f5cc3e35a2661e497bc1468cff6a593f4cf666b1eb7e06d`,
  comparison-report digest
  `e22289df2fbfdd6bddda8ac0776395681bed0d85939f69b955f777274d05a7b9`,
  and review date `2026-08-23`.
- The deterministic T3.2 mapping therefore selects untreated operation while
  recording that Phase 2 could not determine a prompt effect. No prompt-effect
  claim is authorized.
- The worksheet and raw evidence remain private and ignored. Exact
  cohort/reference identity approval and the live T3.3 runner boundary remained
  open at this checkpoint; this closeout authorized no model invocation, pull,
  or live evidence run.

## Owner-approved T3.1 cohort lock

- The owner approved the exact installed identities and order below on
  2026-08-23. Five models are peers; `qwen3.8:27b-mlx` is separately labelled
  as the higher-capability reference.
- The deployed runtime lock is Ollama `0.32.15`. Every selected tag is local,
  versioned, and distinct by full digest; no selected `:latest` alias,
  duplicate bytes, pull, or substitution is permitted.

| Position | Role | Installed tag | Full digest | Family provenance | Artifact / precision |
| ---: | --- | --- | --- | --- | --- |
| 1 | peer | `hf.co/ermiaazarkhalili/LFM2.5-2.6B-SFT-Fable5-Glint-GGUF:Q8_0` | `2c88e114a368b8500aabb7cf32e8a16c274d2265b640c601198a784a559bc5ed` | Ollama architecture `lfm2` | GGUF / Q8_0 |
| 2 | peer | `granite4.1:3b` | `6fd349357287c7ffc9e38189a93b48ea175d24fc566b38f09cfc564fb7f303eb` | Ollama architecture `granite` | GGUF / Q4_K_M |
| 3 | peer | `ministral-3:3b` | `a48e77f25d7933c64552d810c3ca5c7fc8cce4ad7e1ff1432fe24574c8e146e0` | Ollama architecture `mistral3` | GGUF / Q4_K_M |
| 4 | peer | `gemma4:e2b` | `7fbdbf8f5e45a75bb122155ed546e765b4d9c53a1285f62fd9f506baa1c5a47e` | Ollama architecture `gemma4` | GGUF / Q4_K_M |
| 5 | peer | `qwen3.5:0.8b` | `f3817196d142eaf72ce79dfebe53dcb20bd21da87ce13e138a8f8e10a866b3a4` | Ollama architecture `qwen35` | GGUF / Q8_0 |
| 6 | reference | `qwen3.8:27b-mlx` | `5642e97495e1a088883805981563dcdc4a040c2f53388b7a41d1f24d3622cf7e` | Ollama architecture `qwen3_5` | safetensors/MLX / nvfp4 |

- Exact byte sizes, template fingerprints, default-system-prompt fingerprints,
  and advertised tool/thinking metadata are part of the suite-owned lock.
  Template fingerprints are SHA-256 values of the read-only
  `ollama show --template` output. Five default-system outputs were explicitly
  empty; the non-empty `ministral-3:3b` system output is fingerprinted rather
  than published.
- The provider-free approved-preflight path rejects runtime, digest, role,
  order, tag, or metadata drift before evidence allocation. Existing fake
  inventories continue to cover missing models, remote/cloud entries, mutable
  aliases, duplicate bytes, unavailable optional metadata, and mixed artifact
  formats.
- Seed behavior is not exposed by the read-only installed-model inventory and
  is not guessed by the T3.1 identity lock. It remains explicit in the later
  execution contract. No model was invoked, no model was pulled, and no live
  cohort task or evidence directory was created.

Verification:

- `./gradlew :setaccio-lab:toolCompatibilityTest --tests 'com.setaccio.lab.toolcompat.ToolCompatibilityCohort*' --no-daemon`
- `./gradlew :setaccio-lab:toolCompatibilityTest --no-daemon`
- `./gradlew :setaccio-lab:test --no-daemon`
- `./gradlew :setaccio-lab:tasks --group verification --no-daemon`
- `./gradlew :setaccio-lab:check --dry-run --no-daemon`
- `git diff --check`
