# 2026-06-15

## Public lab server benchmark wiring

### Summary
- Reframed `setaccio-lab` as both the public side of the Setaccio split and the inspectable technical showcase for reusable AI/file-processing work.
- Wired `POST /api/lab/vision` through a real Spring AI/Ollama benchmark service instead of the previous `501 NOT_IMPLEMENTED` placeholder.
- Added public-safe lab server primitives for BLAKE3 hashing, MIME detection, multipart temp-file handling, Caffeine-backed result caches, and JSON benchmark result writing.
- Kept `setaccio-core` Spring-free; all Spring wiring remains in `setaccio-lab`.
- Documented the endpoint, output directory, default offline behavior, and test expectations in the public README, environment guide, test plan, AGENTS guidance, and changelog.

### Verification
- `./gradlew :setaccio-core:build :setaccio-lab:test`
- `./gradlew :setaccio-core:build :setaccio-lab:build`

### Notes
- Default builds remain provider-credential-free, Docker-free for `setaccio-lab`, and configured with Ollama model pulling disabled.
- The private `setaccio` repository consumes these public primitives through a Gradle composite build, but private product code, deployment details, and `/api/files/*` compatibility behavior remain outside this repository.

## Local Ollama default model

### Summary
- Changed the default local Ollama chat/vision model from `granite3.2-vision` to `gemma4:e2b`.
- Updated the application config, environment guide, and changelog so the runtime default and public docs agree.

### Verification
- `rg -n "granite3\\.2-vision|gemma4:e2b|OLLAMA_MODEL" setaccio-lab/src/main/resources/application.yml docs/ENVIRONMENT.md CHANGELOG.md docs/logs/2026-06-15.md`
