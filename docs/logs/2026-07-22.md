# 2026-07-22

## July 2026 plan review and Embabel assessment

- Reviewed `plan/julyplan.md` (authored by Codex with comments by Grok).
- Evaluated the Embabel JVM agent framework architectural fit for Setaccio Lab:
  - Agreed with freezing Embabel integration for July due to version alignment risks (Embabel 1.0 managing Spring Boot 3.5 / Spring AI 1.1.7 vs Setaccio Lab on Spring Boot 4.1 / Spring AI 2.0.0).
  - Supported keeping direct Spring AI as the primary baseline for evaluation and benchmarks.
- Reviewed July finish plan and current codebase state (`VisionBenchmarkService` and `ToolSearchMatrixBaselineRunner`):
  - Confirmed the necessity of extracting reusable evidence lifecycle primitives without over-engineering a full framework.
  - Endorsed creating a separate sequential vision matrix runner task to prevent local Ollama contention caused by `VisionBenchmarkService`'s async parallel execution.
  - Highlighted PII/host leakage in `VisionBenchmarkService` (hostname scrubbing needed in evidence lifecycle).
  - Confirmed hash roles (BLAKE3 for inputs, SHA-256 for artifacts) and `never` pull strategy enforcement.
- Added detailed Gemini review comments to `plan/julyplan.md`.

## Agent guidance: follow `.gitignore`

- Updated `AGENTS.md` to require agents to read and follow `.gitignore` before creating, inspecting, or including files.
- Clarified that ignored generated outputs, local-only configuration, credentials, and other untracked files must remain protected unless explicitly instructed otherwise.

### Verification

- `git diff --check`
